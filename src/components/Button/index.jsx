import { Box, Button, Flex } from "@chakra-ui/react";

const CompButton = ({ text }) => {
  return (
    <Button colorScheme="whatsapp" variant="outline">
      {" "}
      {text}{" "}
    </Button>
  );
};
export default CompButton;
